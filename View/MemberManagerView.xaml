<UserControl x:Class="MemberManager.View.MemberManagerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MemberManager.View" 
             xmlns:components="clr-namespace:MemberManager.Components"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0"  Style="{StaticResource PageHeader}" Text="Members"/>
            <Button Grid.Column="1" Content="Add Member" FontSize="16" Command="{Binding AddMemberCommand}"/>
            
        </Grid>
            
        <Grid Grid.Row="1" Margin="0 20 0 0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="2*" />
            </Grid.ColumnDefinitions>

            <components:MemberListing Grid.Column="0" DataContext="{Binding MemberListingViewModel}"/>
            <components:MemberDetails Grid.Column="1" Margin="20 0 0 0" DataContext="{Binding MemberDetailsViewModel}"/>
        </Grid>
    </Grid>
</UserControl>
